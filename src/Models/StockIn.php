<?php

declare(strict_types=1);

// StockIn model abolished. This file remains intentionally empty to avoid autoload errors if referenced by older code.
// All stock entries are now tracked via StockMovement (type = 'in').
